# موسوعة هندسة النظم الشاملة — System Design Mastery Encyclopedia
> **الدليل المرجعي الشامل لتصميم الأنظمة الموزعة الفائقة — من المفاهيم الأساسية إلى معماريات الإنتاج العالمي**

---

## 1. In a Hurry — المسار السريع

### 1.1 المقدمة — Introduction
هندسة وتصميم النظم (System Design) هي عملية تحديد المعمارية (Architecture)، والمكونات (Components)، والواجهات (Interfaces)، ونماذج البيانات (Data Models) لنظام برمجي معقد لتلبية متطلبات وظيفية (Functional Requirements) وغير وظيفية (Non-Functional Requirements) مثل التوسع (Scalability)، والموثوقية (Reliability)، والتوفر (Availability).

* **المستوى 1 (الأساسيات والمفاهيم):** الأنظمة الأحادية (Monolith) تبدأ بقاعدة بيانات واحدة وخادم واحد. مع زيادة عدد المستخدمين، ينشأ عنق الزجاجة (Bottleneck). التوسع ينقسم إلى رأسي (Vertical Scaling - تعزيز المعالج والذاكرة) وأفقي (Horizontal Scaling - إضافة خوادم وتوزيع الحمل عبر Load Balancers).
* **المستوى 2 (المعمارية والمفاضلات):** الانتقال إلى الأنظمة الموزعة والخدمات المصغرة (Microservices) يقدم ميزات الاستقلالية وقابلية التوسع الجزئي، لكنه يفرض تعقيدات شبكية مثل زمن الوصول (Latency)، وتحديات الاتساق (Consistency)، ونقاط الفشل المفردة (SPOF). المفاضلة تدور حول السرعة التشغيلية مقابل تعقيد الصيانة (Operational Overhead).
* **المستوى 3 (الأنظمة الفائقة والإنتاج Staff+):** تقييم المعمارية وفق معايير التوافر العالي 99.999% (Five Nines = أقل من 5 دقائق توقف سنويًا)، مع بناء استراتيجيات التعافي من الكوارث في مراكز بيانات متعددة (Multi-Region Active-Active)، وعزل الإخفاقات (Blast Radius Reduction)، وتصميم النظم لتكون خالية من الحالة (Stateless Services).

---

### 1.2 كيفية الاستعداد — How to Prepare
* **المستوى 1 (بناء الأساس النظري):** فهم البنيات الأساسية: خوادم الويب، خوادم التطبيقات، قواعد البيانات العلائقية وغير العلائقية، الكاش (Redis/Memcached)، وموازنات الأحمال. المراجع الأساسية: *Designing Data-Intensive Applications* لمارتن كليبمان و *System Design Interview* لأليكس شو.
* **المستوى 2 (الحسابات التقديرية وتفكيك المشاكل):** التدرب على حسابات الحجم التقديرية (Back-of-the-envelope calculations) لحساب QPS والتخزين وسعة النطاق الترددي، واستخدام أنماط جاهزة لحل مشاكل التغذية الإخبارية، ومنظومات الرسائل، ونمذجة الواجهات وقواعد البيانات بدقة.
* **المستوى 3 (المحاكاة ومناقشة المقايضات المعقدة):** إجراء مقابلات تجريبية (Mock Interviews)، قراءة مدونات الهندسة للشركات الكبرى (Netflix TechBlog, Uber Engineering, Meta Engineering)، ومناقشة تفاصيل الفشل الواقعية مثل تقسيم الشبكة (Network Partitions)، والسباق التزامني (Race Conditions)، وانهيار الكاش (Thundering Herd).

---

### 1.3 منهجية الحل وتقديم المقابلة — Delivery Framework
إطار زمني معياري لإدارة مقابلة System Design مدتها 45 دقيقة:

| المرحلة | الوقت المقترح | المهام الجوهرية والمخرجات |
| :--- | :--- | :--- |
| **1. فهم وتحديد النطاق (Scope & Requirements)** | 5 - 8 دقائق | تحديد المتطلبات الوظيفية (Core Features)، والمتطلبات غير الوظيفية (Scale, Latency, Availability, Consistency)، وتقدير الأرقام (QPS, Storage). |
| **2. التصميم عالي المستوى (High-Level Design)** | 10 - 15 دقيقة | رسم المكونات الكبرى: Client, CDN, Load Balancer, API Gateway, Microservices, Databases, Cache, Message Queues. تحديد مسار القراءة والكتابة. |
| **3. التعمق في المكونات (Deep Dive)** | 15 - 20 دقيقة | التركيز على أكثر 2-3 مكونات حرجة: خوارزميات التجزئة، هياكل البيانات، تجنب السباق التزامني، تقسيم قواعد البيانات، وتدفق الأحداث. |
| **4. معالجة الاختناقات والخاتمة (Bottlenecks & Wrap-up)** | 3 - 5 دقائق | مناقشة نقاط الفشل المفردة (SPOF)، المراقبة والمقاييس (Metrics/Alerts)، التوسع المستقبلي، وتلخيص المقايضات المتخذة. |

* **المستوى 1:** السيطرة على المتطلبات وتوضيح الافتراضات قبل البدء في الرسم المعماري.
* **المستوى 2:** رسم مخططات البيانات (Schemas) وواجهات REST/gRPC ومسار الطلب بالتفصيل.
* **المستوى 3:** القيادة الفنية، تبرير القرارات المعمارية بالأرقام والبدائل، وتقديم خطط الطوارئ (Graceful Degradation).

---

### 1.4 ملخص المفاهيم الجوهرية — Core Concepts Overview
* **المستوى 1:** DNS، Load Balancers (L4/L7)، Web Servers، Caching، المعاملات العلائقية (ACID).
* **المستوى 2:** Sharding، Replication (Master-Slave/Multi-Master)، Consistent Hashing، Message Queues.
* **المستوى 3:** CAP & PACELC Theorems، خوارزميات التوافق (Raft, Paxos, Zab)، نماذج الاتساق المتقدمة (Linearizable vs Eventual)، والأقفال الموزعة.

---

### 1.5 خريطة التقنيات الأساسية — Key Technologies Matrix

| الفئة | أبرز التقنيات | متى تستخدمها؟ (Use Cases) | نقاط القوة والمفاضلة |
| :--- | :--- | :--- | :--- |
| **In-Memory Cache** | Redis, Memcached | الجلسات، عدادات الإعجاب، Leaderboards، وتخزين الاستعلامات السريعة. | سرعة فائقة (sub-millisecond)، مقيدة بحجم الذاكرة. |
| **Message Broker / Log** | Kafka, RabbitMQ, Pulsar | تدفق الأحداث، معالجة الدفعات، السجلات، والاتصال غير المتزامن. | Kafka: سجل ثابت وترتيب بالأقسام وقابلية استرجاع الأحداث. |
| **Search Engine** | Elasticsearch, OpenSearch | البحث النصي الكامل (Full-text Search)، التصفية المعقدة، وتحليل السجلات. | مبني على Inverted Index، ممتاز في القراءة، ومكلف في التحديث الفوري. |
| **Wide-Column / NoSQL** | Cassandra, ScyllaDB | بيانات السلاسل الزمنية، رسائل الدردشة، كتابة فائقة الكثافة. | كتابة سريعة مع LSM-Trees وتوافر عالٍ بدون نقطة فشل مفردة (Masterless). |
| **RDBMS** | PostgreSQL, MySQL | البيانات المالية، الحسابات البنكية، والبيانات ذات الاتساق الصارم (ACID). | اتساق قوي ودعم علاقات معقدة، وتوسعه الأفقي في الكتابة يتطلب Sharding. |

---

### 1.6 ملخص الأنماط المعمارية الشائعة — Common Patterns Summary
* **CQRS (Command Query Responsibility Segregation):** فصل مسار القراءة عن مسار الكتابة لتحسين أداء وقابلية توسع كل مسار على حدة.
* **Saga Pattern:** إدارة المعاملات الموزعة عبر سلسلة خطوات محلية مع معاملات تعويضية (Compensating Transactions) في حال الفشل بدلاً من 2PC.
* **Outbox Pattern:** ضمان اتساق نشر الرسائل إلى وسيط الرسائل (Kafka) بالتزامن مع حفظ البيانات في قاعدة البيانات داخل معاملة محلية واحدة.
* **Circuit Breaker:** منع انهيار الخدمات المتسلسل (Cascading Failures) بإيقاف إرسال الطلبات مؤقتًا للخدمات المتعطلة وتوفير رد بديل (Fallback).

---

### 1.7 قالب تفكيك الأسئلة — Question Breakdowns Framework
يعتمد تفكيك كل مسألة على تحديد المتطلبات والحسابات التقديرية، ثم التدرج عبر المستويات الثلاثة:
1. **Level 1 (Foundation):** المعمارية المبدئية ومسار البيانات الأساسي.
2. **Level 2 (Scale & Trade-offs):** التوسع، التخزين المؤقت، تقسيم البيانات، وتحسين الأداء.
3. **Level 3 (Staff+ Edge Cases):** التزامن العالي، الأقفال الموزعة، مقاومة الأعطال، والاتساق الصارم.

---

## 2. Core Concepts — المفاهيم الأساسية

### 2.1 أساسيات الشبكات — Networking Essentials
* **المستوى 1 (البروتوكولات ونموذج OSI):** نموذج OSI و TCP/IP من الطبقة الفيزيائية حتى الطبقة السابعة. مقارنة TCP (موثوق، يضمن الترتيب عبر 3-Way Handshake، للويب والملفات) و UDP (عديم الحالة، سريع بدون ضمان وصول، للألعاب والبث المباشر).
* **المستوى 2 (تطور HTTP وموازنة الأحمال):**
  * **HTTP/1.1:** اتصالات مستمرة (Keep-Alive) مع عيب Head-of-Line Blocking على مستوى التطبيق.
  * **HTTP/2:** Multiplexing عبر اتصال TCP واحد مع ضغط الترويسات (HPACK).
  * **HTTP/3 (QUIC):** مبني فوق UDP مع تشفير مدمج يحل مشكلة حجب الحزم كلياً ويدعم Connection Migration.
  * **WebSockets & gRPC:** اتصالات ثنائية الاتجاه مفتوحة (Full-duplex) للأول، واتصال مبني على Protobuf و HTTP/2 للثاني.
  * **Load Balancers (L4 vs L7):** L4 يوجه الحزم بناءً على IP/Port فقط، و L7 يحلل محتوى HTTP (Headers, Cookies, Path) ويتيح SSL Termination.
* **المستوى 3 (التوجيه العالمي واستنزاف المنافذ):** Geo-DNS وتوجيه Anycast لمشاركة نفس عنوان IP عالمياً وحماية النظم من هجمات DDoS. ضبط Connection Pooling لمنع استنزاف منافذ TCP (Socket Exhaustion).

---

### 2.2 تصميم الـ APIs — API Design
* **المستوى 1 (الأنماط الأساسية):** REST (معايير HTTP وأفعالها GET, POST, PUT, DELETE مع أكواد الحالة 200, 201, 400, 404, 500). GraphQL (استعلام مرن يمنع Over/Under-fetching). gRPC (عقود صريحة عبر Protobuf وسرعة فائقة).
* **المستوى 2 (مفاتيح عدم التكرار والتصفح):**
  * **Idempotency Keys:** ترويسة فريدة لمنع تكرار المعاملات المالية عند إعادة المحاولة بسبب انقطاع الشبكة.
  * **Pagination:** تفضيل Cursor/Keyset Pagination (`WHERE id > last_seen_id ORDER BY id ASC LIMIT 20`) على Offset Pagination لتفادي بطء قراءة السجلات السابقة في قواعد البيانات الضخمة.
* **المستوى 3 (التوافقية والتحكم في التدفق):** الحفاظ على التوافق الرجعي (Backward Compatibility)، استخدام Semantic Versioning، ترويسات Rate-Limiting القياسية (`X-RateLimit-Limit`, `Retry-After`)، وإدارة الضغط التراجعي (Backpressure).

---

### 2.3 نمذجة البيانات — Data Modeling
* **المستوى 1 (RDBMS مقابل NoSQL):** قواعد البيانات العلائقية تعتمد على الجداول الصارمة والتطبيع (Normalization) لدعم معاملات ACID. قواعد NoSQL تنقسم إلى Key-Value، Document، Wide-Column، و Graph.
* **المستوى 2 (التطبيع مقابل نزع التطبيع):** في الأنظمة الضخمة، يتم التضحية بالتطبيع (Denormalization) لصالح سرعة القراءة وتجنب عمليات JOIN المكلفة عبر الخوادم. النمذجة في NoSQL تقودها أنماط الاستعلام المسبقة (Access-Pattern Driven Design).
* **المستوى 3 (تصميم الجداول الفردية والأنظمة الهجينة):** تطبيق Single-Table Design في DynamoDB باستخدام مفاتيح مركبة (PK & SK) لتنفيذ جميع استعلامات النظام بطلب شبكي واحد $O(1)$. تبني معمارية Polyglot Persistence بتوزيع المهام حسب طبيعة البيانات (Postgres للماليات، Cassandra للسلاسل، Elastic للبحث).

---

### 2.4 التخزين المؤقت — Caching
* **المستوى 1 (أنماط الكاش):**
  * **Cache-Aside (Lazy Loading):** التطبيق يبحث في الكاش، وإن لم يجد يجلب من قاعدة البيانات ويكتب في الكاش.
  * **Write-Through:** الكتابة في الكاش أولاً ويقوم الكاش بتحديث قاعدة البيانات فوراً.
  * **Write-Back (Write-Behind):** الكتابة في الكاش فوراً، ويتم تفريغ البيانات للقاعدة في دفعات غير متزامنة.
  * **Write-Around:** الكتابة تتجاوز الكاش مباشرة إلى قاعدة البيانات.
* **المستوى 2 (سياسات الإخلاء وحل أزمات الكاش):**
  * سياسات الإخلاء: LRU, LFU, FIFO, ARC.
  * **Cache Stampede / Thundering Herd:** انتهاء مفتاح ساخن يسبب هجوم ملايين الطلبات على القاعدة. الحل: أقفال موزعة (Mutex Lock) أو التجديد الاستباقي (Probabilistic Early Expiration).
  * **Cache Penetration:** طلب مفاتيح غير موجودة إطلاقاً. الحل: Bloom Filters أو تخزين قيم فارغة بـ TTL قصير.
  * **Cache Avalanche:** سقوط خادم الكاش أو انتهاء كل المفاتيح في نفس اللحظة. الحل: إضافة Jitter عشوائي لـ TTL ونشر مجمعات كاش مكررة.
* **المستوى 3 (الكاش متعدد المستويات):** بناء تسلسل هرمي: L1 In-Memory Cache محلي (Caffeine)، يليه L2 Distributed Cache (Redis Cluster)، يليه CDN للملفات الثابتة. استخدام بروتوكولات تفريغ الكاش المعتمدة على سجلات الأحداث (CDC via Debezium).

---

### 2.5 تقسيم قواعد البيانات — Sharding
* **المستوى 1 (الأساسيات):** التقسيم الرأسي (فصل الجداول حسب المجال) مقابل التقسيم الأفقي (توزيع صفوف نفس الجدول عبر خوادم متعددة بناءً على Shard Key).
* **المستوى 2 (استراتيجيات التقسيم والتحديات):**
  * **Range-Based:** التقسيم حسب النطاق (A-E, F-J)؛ عيبه تركز البيانات (Hotspots).
  * **Hash-Based:** `hash(key) % N` لتوزيع متساوٍ؛ عيبه صعوبة إضافة خوادم جديدة.
  * **Directory-Based:** جدول بحث مركزي يحدد موقع كل مفتاح.
  * تحديات Sharding: صعوبة عمليات Cross-shard Joins، وفقد المعاملات الذرية، ومشكلة الحسابات الشهيرة (Celebrity Problem).
* **المستوى 3 (إعادة التقسيم الحي):** تقنيات Zero-Downtime Resharding عبر Virtual Shards، وتقسيم المفاتيح الساخنة بإضافة Salt عشوائي (`user_123_#salt`)، واستخدام قواعد NewSQL الحديثة (Spanner, CockroachDB).

---

### 2.6 التجزئة المتسقة — Consistent Hashing
* **المستوى 1 (الفكرة الأساسية):** ترتيب فضاء الهاش على شكل حلقة دائرية (Hash Ring من 0 إلى $2^{32}-1$)، وتعيين المفتاح إلى أول خادم في اتجاه عقارب الساعة لتجنب إعادة توزيع كافة المفاتيح عند إضافة أو حذف خوادم.
* **المستوى 2 (العقد الافتراضية Virtual Nodes):** تعيين عدة نقاط افتراضية (100-256 نقطة) لكل خادم فيزيائي على الحلقة لضمان توزيع الحمل بالتساوي ومنع البقع الساخنة، مع تقليل نسبة ترحيل البيانات عند تغيير السعة إلى $k/N$ فقط.
* **المستوى 3 (التطبيقات في الإنتاج):** تطبيق التكرار الموزع (Replication Factor = N) بتعيين النسخ الاحتياطية للخوادم الفيزيائية المتتالية عبر مناطق توفر مستقلة (Rack-aware)، واستخدام بروتوكول Gossip لمزامنة حالة الحلقة.

---

### 2.7 نظرية CAP و PACELC
* **المستوى 1 (أضلاع CAP):**
  * **Consistency (C):** كل عملية قراءة تتلقى أحدث كتابة أو خطأ.
  * **Availability (A):** كل عقدة غير فاشلة تعيد استجابة غير خاطئة.
  * **Partition Tolerance (P):** استمرار النظام في العمل رغم انقطاع الرسائل بين العقد (إلزامية في الشبكات الموزعة).
* **المستوى 2 (نظرية PACELC ومستويات الاتساق):**
  * إن وُجد Partition (P)، نفاضل بين التوافر (A) والاتساق (C)؛ وفي غيابه (Else - E)، نفاضل بين زمن الاستجابة (Latency - L) والاتساق (Consistency - C).
  * نماذج الاتساق: Strong, Eventual, Causal, Read-Your-Writes.
* **المستوى 3 (النصاب القانوني Quorum والأختام الزمنية):**
  * معادلة النصاب: $R + W > N$ لضمان الاتساق القوي.
  * معالجة تضارب البيانات عبر Vector Clocks و Last-Write-Wins (LWW) باستخدام أختام زمنية هجينة (Hybrid Logical Clocks - HLC).

---

### 2.8 فهرسة قواعد البيانات — Database Indexing
* **المستوى 1 (الفهارس الأساسية):** الفهرس المجمع (Clustered Index) يحدد الترتيب الفيزيائي الفعلي للصفوف على القرص، بينما الفهرس غير المجمع (Non-Clustered Index) يحتوي مؤشرات تشير إلى موقع الصف الأصلي.
* **المستوى 2 (مقارنة B+ Trees و LSM Trees):**

| المعيار | B+ Tree (InnoDB, Postgres) | LSM Tree (Cassandra, RocksDB) |
| :--- | :--- | :--- |
| **الأمثلية** | القراءة السريعة وعمليات النطاق | الكتابة فائقة الكثافة والتسجيل المستمر |
| **آلية الكتابة** | تحديث مباشر في مكان البيانات في الصفحات (In-place) | تسجيل تسلسلي في الذاكرة (Memtable) ثم تصدير SSTables |
| **مضاعفة الكتابة** | عالية بسبب إعادة كتابة صفحات كاملة (Random I/O) | منخفضة في البداية، وتحدث أثناء عمليات الدمج (Compaction) |
| **الهياكل المرافقة** | صفحات متوازنة ومؤشرات ترابط تسلسلي للأوراق | سجل WAL، و Memtable، وملفات SSTable مع Bloom Filters |

* **المستوى 3 (الفهارس المغطية والمركبة):** تصميم الفهارس المركبة وفق مبدأ أقصى اليسار (Leftmost Prefix Rule). استخدام الفهارس المغطية (Covering Indexes) لقراءة البيانات من الفهرس مباشرة (Index-Only Scan) دون الرجوع للجدول، والفهارس الجزئية (Partial Indexes) لتوفير الذاكرة.

---

### 2.9 الأرقام المهمة وحسابات الحجم — Numbers to Know

| العملية | زمن الاستجابة التقديري | المقارنة والتعليق |
| :--- | :--- | :--- |
| **L1 Cache Reference** | 0.5 - 1 ns | فائق السرعة داخل المعالج |
| **Branch Mispredict / L2 Cache** | 5 - 7 ns | سريع جداً داخل الشريحة |
| **Main Memory (RAM) Reference** | 100 ns | أسرع بنحو 100,000 مرة من القرص الصلب |
| **Send 1 MB over 1 Gbps Network** | 10,000,000 ns (10 ms) | الشبكة هي عنق الزجاجة الأساسي |
| **Read 1 MB sequentially from SSD** | 1,000,000 ns (1 ms) | قراءة تسلسلية سريعة |
| **Read 1 MB sequentially from HDD** | 20,000,000 ns (20 ms) | أبطأ بمراحل في البحث العشوائي |
| **Cross-datacenter roundtrip (US to EU)** | 150,000,000 ns (150 ms) | قيود سرعة الضوء الفيزيائية |

* **المستوى 1 (قواعد التقريب اليومية):** 1 يوم = 86,400 ثانية (يقرب إلى $100,000$ أو $10^5$). 1 مليون طلب يومياً = $10	ext{ QPS}$ في المتوسط.
* **المستوى 2 (حسابات السعة):**
  * معدل الذروة (Peak QPS) = متوسط QPS $	imes$ معامل أمان (2x إلى 5x).
  * سعة التخزين لـ 5 سنوات = $	ext{Daily Writes} 	imes 	ext{Size per Record} 	imes 365 	imes 5 	imes 	ext{Replication Factor (3x)}$.
  * سعة الكاش (قاعدة 80/20) = تخزين 20% من البيانات الأكثر قراءة لتغطية 80% من حركة المرور.
* **المستوى 3 (النطاق الترددي واستيعاب الذاكرة):**
  * عرض النطاق الترددي (Gbps) = $rac{	ext{QPS} 	imes 	ext{Payload Bytes} 	imes 8}{10^9}$.
  * عدد خوادم التطبيقات المطلوبة = $	ext{Concurrency} = 	ext{QPS} 	imes 	ext{Latency per Request}$.

---

## 3. Question Breakdowns — مسائل وتطبيقات عملية (31 مسألة)

### 3.1 تقصير الروابط — Bitly (URL Shortener)
* **المتطلبات والحسابات:** 100M رابط جديد شهرياً (38 Write QPS)، نسبة القراءة للكتابة 100:1 (Read QPS $pprox$ 3,800). التخزين لـ 5 سنوات: 6 مليار رابط $	imes$ 500 بايت $pprox$ 3 تيرابايت.
* **المستوى 1:** توليد مفتاح بطول 7 أحرف باستخدام Base62 الذي يوفر $62^7 pprox 3.5	ext{ Trillion}$ احتمال. استخدام جدول علائقي `(id, short_url, original_url, created_at)`.
* **المستوى 2:** إنشاء خدمة توليد المفاتيح مسبقاً (Key Generation Service - KGS) وتخزينها في الذاكرة لتسليمها بـ $O(1)$. تخزين الروابط الساخنة في Redis، واستخدام HTTP 301 للتوجيه الدائم أو HTTP 302 لجمع التحليلات.
* **المستوى 3:** تقسيم قاعدة البيانات بـ `hash(short_key) % N`، حماية المنظومة من الروابط الضارة (Google Safe Browsing API)، واستخدام CDN Caching عند الـ Edge للروابط الفيروسية مع تفريغ إحصائيات النقرات إلى Kafka ثم ClickHouse.

---

### 3.2 التخزين والمزامنة السحابية — Dropbox
* **المتطلبات:** رفع وتنزيل ومزامنة الملفات، حفظ الإصدارات، ودعم استئناف الرفع عند انقطاع الاتصال.
* **المستوى 1:** فصل البيانات الوصفية (Metadata في MySQL) عن محتوى الملفات الثنائي (Binary Blobs في S3). تقطيع الملفات لأجزاء (Chunks بحجم 4MB) وتوليد SHA-256 لمنع تكرار تخزين الأجزاء المتطابقة (Deduplication).
* **المستوى 2:** عميل مزامنة ذكي (Sync Client) يراقب نظام الملفات المحلي، ويحسب الفروقات (Rolling Hash / Rsync) لرفع الأجزاء المعدلة فقط (Delta Sync). خادم تنبيهات عبر WebSockets/Long Polling لإشعار باقي أجهزة المستخدم بالتحديثات.
* **المستوى 3:** حل تضارب التعديلات المتزامنة (Conflict Resolution) بإنشاء نسخ متفرعة `Conflicted Copy`، وتطبيق التشفير الشامل من جانب العميل، وإلغاء التكرار على النطاق العام (Cross-user Global Deduplication).

---

### 3.3 خدمات التوصيل المحلي — Local Delivery (DoorDash)
* **المتطلبات:** تتبع موقع المندوب بالزمن الحقيقي، مطابقة الطلبات مع أقرب مندوب، وحساب زمن الوصول المتوقع (ETA).
* **المستوى 1:** دورة حياة الطلب: تم الإنشاء $ightarrow$ تأكيد المطعم $ightarrow$ تعيين المندوب $ightarrow$ استلام $ightarrow$ في الطريق $ightarrow$ تم التسليم. تخزين الحالات في PostgreSQL مع تقييد جغرافي أولي.
* **المستوى 2:** محرك التوزيع المجمع (Batch Dispatch Engine): تجميع الطلبات في نوافذ زمنية (30 ثانية) وتطبيق خوارزمية المطابقة الموزونة (Bipartite Matching) لتقليل زمن الانتظار. تتبع مواقع المندوبين كل 4 ثوانٍ في Redis Geospatial.
* **المستوى 3:** حساب ETA المركب (زمن تجهيز الطعام بالـ ML + زمن وصول المندوب + حركة المرور اللحظية). إدارة إلغاء المندوب للطلب تلقائياً وإعادة جدولته بأولوية قصوى دون إعادة طبخ الطعام.

---

### 3.4 حجز التذاكر والفعاليات الكبرى — Ticketmaster
* **المتطلبات:** مبيعات خاطفة (Flash Sales) لمليون مستخدم في ثوانٍ، منع الحجز المزدوج للمقعد، وقفل المقعد مؤقتاً لمدة 10 دقائق أثناء الدفع.
* **المستوى 1:** جدول المقاعد في قاعدة البيانات مع قفل تفاؤلي (Optimistic Locking) باستخدام عمود `version`.
* **المستوى 2:** غرفة الانتظار الافتراضية (Virtual Waiting Room) لتمرير دفعات بمعدل ثابت يطابق سعة الدفع. تنفيذ القفل الذري للمقعد في Redis بنصوص Lua مع TTL لمدة 10 دقائق: `SET seat_123 user_456 NX EX 600`.
* **المستوى 3:** إدارة معاملات الدفع عبر Saga Pattern، مع إعادة المقعد فوراً لحالة المتاح عبر حدث في Kafka عند فشل الدفع أو انقضاء المهلة. الحماية من البوتات عبر Proof-of-Work و Cloudflare Rate Limiting.

---

### 3.5 شريط الأخبار — Facebook News Feed
* **المتطلبات:** نشر المنشورات، وتوليد وتصفح شريط الأخبار بزمن وصول $< 200	ext{ ms}$ مع دعم المشاهير.
* **المستوى 1:** نموذج السحب الساذج (Pull Model) باستعلام `SELECT ... WHERE user_id IN (friends)`؛ بطيء جداً $O(N)$ مع كثرة الأصدقاء.
* **المستوى 2:** نموذج التوزيع المسبق (Push / Fan-out-on-write): عند النشر، تُحقن معرفات المنشور في Feed Cache الخاص بكل صديق في Redis لتصبح القراءة $O(1)$.
* **المستوى 3:** النموذج الهجين (Hybrid Fanout): Push للمستخدمين العاديين، و Pull للمشاهير والحسابات المليونية عند فتح التغذية لتقليل تكلفة الكتابة. ترتيب المنشورات بنماذج تعلم الآلة (Ranking Engine) بناءً على التفاعل والحداثة.

---

### 3.6 منصات التوافق الجغرافي — Tinder
* **المتطلبات:** التمرير (Swipe Left/Right)، مطابقة التوافق الجغرافي، وإشعار الطرفين فوراً عند حدوث Mutual Match.
* **المستوى 1:** حفظ الإعجابات في جدول قاعدة بيانات، وإنشاء سجل Match عند تطابق رغبة الطرفين.
* **المستوى 2:** تقسيم العالم لخلايا جغرافية عبر Uber H3 / Google S2. توليد طابور توصيات مسبق (50-100 ملف شخصي) لكل مستخدم في Redis لتوفير تجربة سحب فورية.
* **المستوى 3:** معالجة مليارات عمليات السحب بدفع الأحداث إلى Kafka، واستخدام Bloom Filters لمنع ظهور الملفات المعروضة سابقاً، وحساب درجات الجاذبية (Elo Score) في الخلفية.

---

### 3.7 منصات تقييم الأكواد — LeetCode
* **المتطلبات:** إرسال الكود، تجميعه وتنفيذه ضد مدخلات الاختبار، ومقارنة المخرجات في بيئة معزولة وآمنة.
* **المستوى 1:** استقبال الكود عبر API، وضعه في طابور مهام (RabbitMQ)، وسحبه بواسطة Worker لتنفيذه وإرجاع النتيجة.
* **المستوى 2:** بيئة عزل آمنة (Sandboxing via Docker, Linux cgroups, namespaces, gVisor). تقييد الذاكرة (256MB)، والمعالج (2s)، وحظر الوصول إلى الشبكة تماماً لمنع الاختراقات وهجمات Fork Bombs.
* **المستوى 3:** تحميل حالات الاختبار الكبيرة مسبقاً في الذاكرة المشتركة (Shared Memory)، والتوسع التلقائي لعقد العمال (Auto-scaling Workers) في المسابقات، وبث نتائج الاختبارات لحظياً عبر SSE.

---

### 3.8 المحادثات الفورية المشفرة — WhatsApp
* **المتطلبات:** رسائل فورية، تشفير شامل (E2EE)، دعم المجموعات، وحالة الاتصال والظهور.
* **المستوى 1:** اتصالات مستمرة عبر WebSockets/XMPP. الخادم يمرر الرسالة للمستلم إن كان متصلاً، أو يحفظها في طابور غير متصل (Offline Queue).
* **المستوى 2:** التشفير الشامل عبر Signal Protocol (Double Ratchet). الخادم لا يملك مفاتيح فك التشفير. حذف الرسائل نهائياً من الخوادم فور تأكيد استلامها (Double Tick).
* **المستوى 3:** إدارة المجموعات بالتوزيع من جانب الخادم (Server-side fan-out). خدمة حالة الظهور (Presence) تعتمد على نبضات قلب دورية كل 5 ثوانٍ في Redis Bitmaps لتفادي إغراق الشبكة.

---

### 3.9 محدد معدل الطلبات — Rate Limiter
* **المتطلبات:** منع إساءة استخدام الـ APIs وحماية الخدمات بتحديد عدد الطلبات لكل مستخدم/IP.
* **المستوى 1:** تطبيق خوارزمية Token Bucket محلياً في ذاكرة الخادم وإرجاع كود `429 Too Many Requests`.
* **المستوى 2:** التوزيع عبر Redis Cluster باستخدام خوارزمية Sliding Window Counter وتنفيذ المنطق ذرياً عبر نصوص Redis Lua Scripts لمنع مشاكل السباق التزامني.
* **المستوى 3:** معمارية متعددة المستويات: مزامنة دفعات الرموز محلياً على خوادم التطبيقات مع مزامنة غير متزامنة مع Redis، وتطبيق التقييد على مستوى API Gateway (Envoy/Kong) و Edge CDN.

---

### 3.10 منصات بث ومشاركة الفيديو — YouTube
* **المتطلبات:** رفع الفيديوهات، معالجتها بدقات متعددة، وتوزيعها للبث التكيفي عالمياً.
* **المستوى 1:** رفع الفيديو لـ S3 $ightarrow$ تحويله بواسطة Transcoder لدقات مختلفة $ightarrow$ البث عبر مشغل الويب.
* **المستوى 2:** خط معالجة موزع (Chunked Transcoding Pipeline): تقطيع الفيديو لقطع صغيرة (GOPs مدتها 5-10 ثوانٍ) ومعالجتها بالتوازي عبر مئات خوادم GPU. دعم البث التكيفي (HLS/DASH).
* **المستوى 3:** التخزين المؤقت للفيديوهات الشهيرة في Edge CDNs القريبة من مزودي الإنترنت (ISPs). ترحيل الفيديوهات نادرة المشاهدة للتخزين البارد مع التشفير المضغوط (Erasure Coding).

---

### 3.11 التعليقات المباشرة للبث — Facebook Live Comments
* **المتطلبات:** بث مباشر يتابعه الملايين يكتبون مئات آلاف التعليقات في الثانية بزمن وصول $< 500	ext{ ms}$.
* **المستوى 1:** اتصال المشاهدين بخادم WebSockets ونشر التعليقات (Broadcast) لكافة المتصلين.
* **المستوى 2:** تطبيق شجرة خوادم توزيع (Edge Relay Tree) وتطبيق Sampling/Throttling لعرض 20-30 تعليق في الثانية فقط على شاشة الهاتف لمنع تجميد واجهة المستخدم.
* **المستوى 3:** حفظ التعليقات الحية في الذاكرة للبث المباشر، ودفع دفعات إلى Kafka لتخزينها في Cassandra مصحوبة بالطابع الزمني الدقيق للتشغيل المتزامن في وضع التسجيل (VOD Replay).

---

### 3.12 حساب العناصر الأكثر مشاهدة — YouTube Top K Heavy Hitters
* **المتطلبات:** حساب أكثر $K$ فيديوهات مشاهدة خلال نافذة زمنية من بين مليارات المشاهدات.
* **المستوى 1:** استخدام HashMap لكل فيديو وفرز النتائج بالـ Heap؛ ينهار سريعاً لاستهلاكه ذاكرة هائلة.
* **المستوى 2:** هياكل بيانات احتمالية: **Count-Min Sketch** لتقدير تردد المشاهدات باستهلاك ذاكرة ثابت وصغير، مع الاحتفاظ بـ **Min-Heap** بحجم $K$ لأفضل الفيديوهات.
* **المستوى 3:** المعالجة المتوازية عبر Apache Flink عبر نوافذ زمنية منزلقة، ودمج نتائج الـ Top K المحلية في مجمّع نهائي (Global Top K Reducer) وتخزين الناتج في Redis Sorted Set.

---

### 3.13 تطبيقات النقل التشاركي — Uber / Lyft
* **المتطلبات:** تتبع مواقع السائقين بالزمن الحقيقي ومطابقة الراكب مع أقرب سائق بدقة.
* **المستوى 1:** تحديث إحداثيات السائق كل 4 ثوانٍ عبر WebSocket وحفظها في قاعدة البيانات والاستعلام بالنطاق.
* **المستوى 2:** الفهرسة المكانية عبر **Uber H3** (شبكة سداسية متداخلة). تخزين مواقع السائقين في الذاكرة حسب رقم الخلية السداسية وفحص الخلية الحالية والخلايا المجاورة فوراً.
* **المستوى 3:** حساب التسعير الديناميكي (Surge Pricing) بناءً على العرض والطلب في كل خلية H3 كل 10 ثوانٍ. قفل السائق في Redis لمدة 10 ثوانٍ عند إرسال العرض لمنع تضارب المطابقات.

---

### 3.14 زاحف الويب الشامل — Distributed Web Crawler
* **المتطلبات:** تنزيل مليارات صفحات الويب واستخراج الروابط وفهرستها مع تجنب الحلقات اللانهائية.
* **المستوى 1:** دورة زحف أساسية: Seed URLs $ightarrow$ Fetch Page $ightarrow$ Parse Links $ightarrow$ Enqueue Unvisited Links.
* **المستوى 2:** بنية **URL Frontier**: طوابير مقسمة حسب النطاق (Domain Queues) لاحترام قواعد الأدب ومعدلات `robots.txt`، واستخدام **Bloom Filter** لفحص الروابط المزارة مسبقاً.
* **المستوى 3:** كشف المحتوى المتطابق أو شبه المتطابق عبر خوارزمية **SimHash / MinHash**، وتوزيع مهام الزحف على آلاف الخوادم باستخدام Kafka و RocksDB لتخزين الحالات محلياً.

---

### 3.15 مجمّع نقرات الإعلانات — Ad Click Aggregator
* **المتطلبات:** تسجيل نقرات الإعلانات، تجميع الإحصائيات لحظياً، ومنع الاحتيال والمحاسبة الخاطئة.
* **المستوى 1:** تسجيل النقرات في جدول واستخراج التقارير بمهمة Cron دورية كل ساعة.
* **المستوى 2:** مسار تدفق مباشر: توجيه النقرات إلى Kafka $ightarrow$ المعالجة بـ Apache Flink عبر نوافذ زمنية منزلقة وتراكمية $ightarrow$ تخزين المخرجات في ClickHouse لتغذية لوحات المعلنين.
* **المستوى 3:** تطبيق ضمانات المعالجة لمرة واحدة بالضبط (Exactly-Once Semantics via Two-Phase Commit & Checkpoints)، واكتشاف النقر الاحتيالي (Click Fraud) بالتحقق من بصمة الجهاز في Redis.

---

### 3.16 البحث في المنشورات الاجتماعية — Facebook Post Search
* **المتطلبات:** بحث نصي لحظي سريع في مليارات المنشورات الاجتماعية مع احترام خصوصية المستخدمين.
* **المستوى 1:** تقطيع المنشورات لكلمات مفتاحية وبناء فهرس معكوس (Inverted Index) بسيط يربط الكلمة بالمنشورات.
* **المستوى 2:** تقسيم الفهرس المعكوس بناءً على معرف المستند (Document-based Partitioning)، وتوجيه استعلام البحث بالتوازي لكافة الأقسام وتجميع النتائج عبر Query Aggregator.
* **المستوى 3:** خط فهرسة مزدوج: فهرس لحظي في الذاكرة للمنشورات الحديثة جداً يدمج مع الفهرس التاريخي، مع تصفية النتائج بناءً على صلاحيات الخصوصية (Privacy Filters) ونماذج الترتيب المخصصة.

---

### 3.17 دليل الأماكن والأنشطة — Yelp
* **المتطلبات:** البحث عن الأماكن والمطاعم والأنشطة القريبة وتصفيتها حسب التقييم والمسافة.
* **المستوى 1:** استعلام SQL يحدد مستطيل الإحداثيات (Bounding Box Query).
* **المستوى 2:** الفهرسة الهرمية عبر **QuadTree** (تجزئة المربعات لأربعة مربعات فرعية كلما زادت الكثافة) أو Geohash للبحث السريع بمطابقة البادئة النصية (Prefix Match).
* **المستوى 3:** تخزين شجرة QuadTree بالكامل في ذاكرة خوادم متماثلة وموزعة جغرافياً (نظراً لأن القراءة تفوق الكتابة بنسبة 1000:1)، ومزامنة التقييمات عبر Kafka بشكل غير متزامن.

---

### 3.18 مشاركة الصور والقصص المؤقتة — Instagram
* **المتطلبات:** رفع الصور والوسائط، شريط متابعة، وقصص مؤقتة تختفي بعد 24 ساعة.
* **المستوى 1:** رفع الصورة إلى S3، وتخزين مسارها وبياناتها في MySQL، وإضافتها لتغذية المتابعين.
* **المستوى 2:** تخزين القصص في Redis/DynamoDB مع ضبط مهلة انتهاء صلاحية تلقائية (`TTL = 24h`). رفع الوسائط مباشرة لـ S3 عبر Presigned URLs لتخفيف الحمل عن خوادم التطبيقات.
* **المستوى 3:** تحويل الوسائط لصيغ حديثة (WebP/AVIF) بعدة أحجام، وتنزيل المحتوى مسبقاً في خلفية تطبيق الهاتف (Prefetching) لتوفير تصفح فوري وسلس.

---

### 3.19 تتبع الأنشطة الرياضية — Strava
* **المتطلبات:** استقبال مسارات GPS، مطابقة المقاطع الرياضية (Segments)، وتوليد لوحات الشرف.
* **المستوى 1:** رفع ملفات GPX/FIT وحفظ نقاط الإحداثيات وحساب المسافة ومتوسط السرعة.
* **المستوى 2:** تبسيط مسار الـ GPS باستخدام خوارزمية **Ramer-Douglas-Peucker** لتقليل النقاط بنسبة 90%، وتصفية المقاطع المرشحة جغرافياً ومطابقتها بخوارزمية Frechet Distance.
* **المستوى 3:** توليد لوحات الصدارة والترتيب اللحظي (Leaderboards) عبر **Redis Sorted Sets** (`ZADD segment_id time athlete_id`) وتحديث لقب KOM بزمن $O(\log N)$.

---

### 3.20 نظام التخزين المؤقت الموزع — Distributed Cache
* **المتطلبات:** بناء نظام كاش موزع عالي التوافر ومنخفض زمن الاستجابة مماثل لـ Redis/Memcached.
* **المستوى 1:** جدول تجزئة في الذاكرة (Hash Table) مدعوم بقائمة مزدوجة الربط لتطبيق سياسة الإخلاء LRU بـ $O(1)$.
* **المستوى 2:** توزيع البيانات عبر العقد باستخدام **Consistent Hashing with Virtual Nodes** مع عميل ذكي (Smart Client) يوجه الطلب مباشرة للخادم المستهدف.
* **المستوى 3:** بنية Master-Replica مع مزامنة غير متزامنة، واستخدام بروتوكول **Gossip** لتبادل الحالة واكتشاف العقد المعطلة وتفعيل التبديل التلقائي (Failover).

---

### 3.21 المزادات العلنية عبر الإنترنت — Online Auction (eBay)
* **المتطلبات:** مزايدة حية، إدارة التنافس في الثواني الأخيرة (Sniping)، وإنهاء المزاد في وقت محدد بدقة.
* **المستوى 1:** إنشاء المزاد وتقديم عروض أعلى من السعر الحالي وتحديد الفائز عند انتهاء الوقت.
* **المستوى 2:** تنفيذ المزايدة ذرياً في الذاكرة عبر نصوص Redis Lua أو القفل المتشائم (`SELECT FOR UPDATE`) لضمان صحة الترتيب، وبث الأسعار لجميع المتصلين عبر WebSockets.
* **المستوى 3:** طوابير تأخير دقيقة (Delayed Queues) لإنهاء المزاد في المللي ثانية المحددة، تمديد الوقت تلقائياً (Soft Close) عند المزايدة في آخر 30 ثانية، وبدء تحصيل الدفع عبر Saga Pattern.

---

### 3.22 مجدول المهام الموزع — Distributed Job Scheduler
* **المتطلبات:** جدولة وتنفيذ ملايين المهام في أوقات محددة بدقة مع ضمان التنفيذ مرة واحدة على الأقل.
* **المستوى 1:** جدول مهام في قاعدة بيانات ومؤقت دوري يفحص المهام المستحقة وينفذها.
* **المستوى 2:** تخزين المهام في **Redis Sorted Set** حيث الـ Score هو الطابع الزمني `timestamp`. خدمة التوزيع تسحب المهام المستحقة `ZRANGEBYSCORE` وتدفعها لطوابير RabbitMQ/Kafka.
* **المستوى 3:** تقسيم طوابير الجدولة (Bucket Partitioning) لمنع اختناق المفاتيح، واستخدام محركات سير العمل (مثل Temporal) لإدارة المهام المعقدة، والتعامل مع حالات سقوط العمال عبر نبضات القلب وإعادة المحاولة بتراجع أسي.

---

### 3.23 مجمّع الأخبار الذكي — News Aggregator (Google News)
* **المتطلبات:** جمع خلاصات الأخبار، إزالة التكرار، وتجميع المقالات حول نفس القصة مع التخصيص.
* **المستوى 1:** سحب دوري لروابط RSS والمواقع وتخزين النصوص وتصنيفها يدوياً حسب القسم.
* **المستوى 2:** توليد تضمينات دلالية (Embeddings via NLP) وتجميع المقالات المتشابهة في عنقود واحد (Story Cluster) بخوارزميات TF-IDF أو HDBSCAN لمنع تكرار نفس الخبر.
* **المستوى 3:** حساب درجات الجدارة الإخبارية (Freshness & Authority) وتوليد خلاصات مخصصة لكل مستخدم في Redis، وتوزيع الأخبار العاجلة عبر CDN Edge مع التحديث بـ SSE.

---

### 3.24 تتبع وتقلبات الأسعار — Price Tracking
* **المتطلبات:** تتبع أسعار المنتجات عبر المتاجر، تسجيل السجل التاريخي، وإرسال تنبيهات فورية عند انخفاض السعر.
* **المستوى 1:** سحب صفحة المنتج دورياً ومقارنة السعر بالحد المطلوب وإرسال بريد إلكتروني.
* **المستوى 2:** جدولة سحب تكيفية (Adaptive Scraping): فحص المنتجات المتقلبة كل 15 دقيقة والمنتجات المستقرة يومياً. تخزين الأسعار في قاعدة بيانات سلاسل زمنية (TimescaleDB / InfluxDB).
* **المستوى 3:** إدارة شبكة وكلاء (Proxy Rotation) لتجاوز حظر البوتات، ومطابقة انخفاض الأسعار عبر محرك قواعد لحظي في Flink لإطلاق مئات آلاف الإشعارات عبر Kafka.

---

### 3.25 منصة الإشعارات الشاملة — Notification System
* **المتطلبات:** إرسال ملايين الإشعارات عبر قنوات متعددة (Push, SMS, Email) مع مراعاة التفضيلات.
* **المستوى 1:** إرسال الطلب مباشرة إلى بوابات الطرف الثالث (APNs, FCM, Twilio, SendGrid).
* **المستوى 2:** فصل الطوابير حسب الأولوية (High Priority لرموز OTP، و Low Priority للتسويق)، وتطبيق خدمة تفضيلات المستخدم لفحص أوقات عدم الإزعاج (Quiet Hours).
* **المستوى 3:** منع تكرار الإشعارات في Redis (Deduplication)، وتجميع الإشعارات المتقاربة (Digest/Batching) في إشعار مجمع واحد لتفادي إزعاج المستخدم.

---

### 3.26 تداول الأسهم والعملات — Robinhood
* **المتطلبات:** تنفيذ أوامر التداول بالزمن الحقيقي، اتساق مالي صارم، وبث أسعار السوق اللحظية.
* **المستوى 1:** استقبال أمر التداول، التحقق من الرصيد، وتوجيه الأمر للسوق وتحديث المحفظة.
* **المستوى 2:** محرك مطابقة أوامر في الذاكرة (In-Memory Matching Engine) بترتيب السعر والوقت (FIFO). تطبيق دفتر الأستاذ المزدوج (Double-Entry Ledger) لضمان توازن الحسابات مديناً ودائناً.
* **المستوى 3:** بث أسعار السوق (Market Ticks) عبر WebSockets مدعومة بـ UDP Multicast داخل مراكز البيانات، وحفظ الأوامر في سجل ثابت غير قابل للتعديل (Append-Only Event Store via Raft).

---

### 3.27 المحررات التشاركية الحية — Google Docs
* **المتطلبات:** تحرير متزامن للمستندات بين عدة مستخدمين بالزمن الحقيقي مع حل تضارب التعديلات.
* **المستوى 1:** اتصال WebSocket وإرسال عمليات الكتابة `Insert(pos, char)` وتوزيعها على المشاركين.
* **المستوى 2:** خوارزمية التحويل التشغيلي (Operational Transformation - OT) لتعديل إحداثيات العمليات المتزامنة وضمان اتساق النص على كافة الأجهزة، أو استخدام CRDTs للأنظمة اللامركزية.
* **المستوى 3:** دعم التحرير دون اتصال (Offline Editing) بإنشاء طابور محلي للعمليات ومزامنتها بالترتيب عند استعادة الاتصال، وتخزين لقطات دورية (Snapshots) مع شجرة العمليات للتراجع الزمني (Undo/Redo).

---

### 3.28 بوابات ومعالجة المدفوعات — Payment System (Stripe)
* **المتطلبات:** معالجة آمنة للمدفوعات، منع الخصم المزدوج، والتسوية المالية مع البنوك.
* **المستوى 1:** استدعاء بوابة الدفع البنكية وتخزين النتيجة في قاعدة البيانات وإرجاع الإيصال.
* **المستوى 2:** طبقة Idempotency صارمة في Redis لمنع تكرار المعاملات. تطبيق معمارية Saga لتنفيذ خطوات الدفع مع معاملات تعويضية لاسترداد الأموال عند الفشل.
* **المستوى 3:** محرك التسوية اليومية (Daily Reconciliation) لمطابقة سجلات النظام الداخلية مع كشوف الحسابات المصرفية الفعلية لاكتشاف وتصحيح الفروقات، والالتزام بمعايير PCI-DSS.

---

### 3.29 مراقبة المقاييس والتنبيهات — Metrics Monitoring (Datadog/Prometheus)
* **المتطلبات:** جمع وتخزين مقاييس أداء الخوادم بالمليارات وإطلاق التنبيهات ورسم لوحات المراقبة.
* **المستوى 1:** وكيل (Agent) يجمع استهلاك الذاكرة والمعالج ويرسله لخادم مركزي ورسمه بـ Grafana.
* **المستوى 2:** خط استقبال عبر Kafka $ightarrow$ التخزين في قاعدة بيانات سلاسل زمنية (Prometheus TSDB) تعتمد على خوارزمية ضغط **Gorilla Float Compression** لتقليل الحجم بنسبة 90%.
* **المستوى 3:** سياسات تقليل العينات التلقائي (Downsampling): بيانات ثوانٍ لـ 7 أيام، ودقائق لـ 30 يوماً، وساعات لـ سنة. محرك تنبيهات موزع يقيّم قواعد PromQL مع كبح التنبيهات المكررة.

---

### 3.30 منصات الشطرنج المباشرة — Online Chess
* **المتطلبات:** نقل الحركات بالزمن الحقيقي، إدارة ساعات اللعب، التوفيق (Matchmaking)، وحساب تقييم ELO.
* **المستوى 1:** نقل الحركات بصيغة الجبر (Algebraic Notation) عبر WebSocket والتحقق من صحتها على الخادم.
* **المستوى 2:** تخزين حالة الرقعة بتنسيق **FEN** في Redis. إدارة توقيت المباراة من جانب الخادم لمنع التلاعب، وحساب تقييم ELO وتحديثه ذرياً عند نهاية المباراة.
* **المستوى 3:** محرك توفيق سريع يجمع اللاعبين في مجمعات تقييم متقاربة ($\pm 50$) باستخدام Redis Sorted Sets. تحليل الحركات بعد المباراة ومقارنتها بمحرك Stockfish لاكتشاف الغش وحظر المخالفين.

---

### 3.31 تقديم نماذج الذكاء الاصطناعي — ChatGPT / LLM Serving
* **المتطلبات:** استقبال استفسارات المستخدمين، تشغيل استدلال النماذج اللغوية الكبيرة، وبث الإجابات لحظياً.
* **المستوى 1:** استقبال الـ Prompt عبر HTTP وبث الرموز (Tokens) المولدة رمزاً برمز عبر Server-Sent Events (SSE).
* **المستوى 2:** التجميع التكراري المستمر (Continuous / Iteration-level Batching) لإدراج الطلبات الجديدة في كل خطوة توليد. استخدام تقنية **PagedAttention (vLLM)** لتجزئة ذاكرة KV Cache ومنع إهدار VRAM.
* **المستوى 3:** التخزين المؤقت الدلالي (Semantic Caching via Vector Search): إعادة الإجابات المخزنة للاستفسارات المتطابقة دلالياً لتوفير كلفة GPU. توجيه الطلبات عبر Model Router لنماذج صغيرة أو كبيرة حسب التعقيد.

---

## 4. Patterns — الأنماط المعمارية للأنظمة الموزعة

### 4.1 التحديثات اللحظية — Real-time Updates
* **المستوى 1:** مقارنة Long Polling، Server-Sent Events (SSE - أحادي الاتجاه للخادم)، و WebSockets (ثنائي الاتجاه).
* **المستوى 2:** معمارية **WebSocket Gateway Tier** لإدارة مئات آلاف اتصالات TCP المفتوحة في طبقة مستقلة، متصلة بـ **Redis Pub/Sub** أو **Kafka** كـ Message Bus مركزي لتوجيه الرسائل للخادم الصحيح.
* **المستوى 3:** التعامل مع Half-open connections بنبضات قلب دورية (Heartbeats كل 30 ثانية). تطبيق **Exponential Backoff with Full Jitter** عند انقطاع الاتصال الجماعي لمنع ظاهرة Thundering Herd.

---

### 4.2 التعامل مع التنافس على الموارد — Dealing with Contention
* **المستوى 1:** القفل المتشائم (`SELECT FOR UPDATE`) يضمن الأمان لكنه يبطئ النظام؛ القفل التفاؤلي (`WHERE version = 1`) ممتاز للتنافس المنخفض.
* **المستوى 2:** الأقفال الموزعة في Redis (Redlock) بمهلة إيجار (Lease TTL)، أو تنفيذ التعديلات ذرياً في الذاكرة عبر **Redis Lua Scripts** دون الحاجة لأقفال شبكية ثقيلة.
* **المستوى 3:** في حالات التنافس الشديدة (Flash Sales)، يتم التخلي عن الأقفال واستخدام نمط **Queue Decoupling** بتوجيه الطلبات لطابور أحادي المسار (Single-threaded Worker أو Actor Model) لمعالجتها تسلسلياً بـ $O(1)$ في الذاكرة.

---

### 4.3 العمليات متعددة الخطوات — Multi-step Processes
* **المستوى 1:** فشل بروتوكول Two-Phase Commit (2PC) في الخدمات المصغرة لتسببه في شلل النظام وبطء الشبكة.
* **المستوى 2:** نمط **Saga Pattern**:
  * **Choreography:** الخدمات تتواصل عبر أحداث Kafka (مناسب للعمليات البسيطة 2-4 خطوات).
  * **Orchestration:** منسق مركزي (Saga Orchestrator) يدير شجرة الحالات والأوامر (أفضل للعمليات المعقدة).
* **المستوى 3:** تطبيق المعاملات التعويضية (Compensating Transactions) بالتراجع العكسي عند الفشل. استخدام نمط **Transactional Outbox Pattern** مع CDC لضمان حفظ الحدث محلياً في SQL ونشره لـ Kafka دون ضياع.

---

### 4.4 توسيع عمليات القراءة — Scaling Reads
* **المستوى 1:** توجيه الكتابة لـ Master DB وتوزيع القراءة عبر عدة نسخ متماثلة (Read Replicas).
* **المستوى 2:** تطبيق نمط **CQRS** لفصل نموذج القراءة غير المطبع (Denormalized Views في Elasticsearch) عن نموذج الكتابة المتسق، مع كاش هرمي متعدد الطبقات.
* **المستوى 3:** معالجة معضلة تأخر النسخ (Replication Lag) بتوجيه قراءات المستخدم لنفس سجله المعدل حديثاً إلى الـ Master مباشرة لمدة محددة (Read-Your-Own-Writes Consistency).

---

### 4.5 توسيع عمليات الكتابة — Scaling Writes
* **المستوى 1:** تجميع سجلات الكتابة في دفعات (Batching) بدلاً من استعلامات فردية لتقليل زمن استدعاء الشبكة وحركات القرص.
* **المستوى 2:** التقسيم الأفقي (Sharding)، واستخدام قواعد بيانات تعتمد على **LSM-Trees** (Cassandra/RocksDB) لتحويل الكتابة لتسجيل تسلسلي فائق السرعة في الذاكرة والـ WAL.
* **المستوى 3:** امتصاص طوفان الكتابة بالكتابة غير المتزامنة (Write-Behind): استقبال الطلبات في Kafka وإرجاع `202 Accepted` فوراً للعميل، وتفريغ البيانات تدريجياً في قواعد البيانات.

---

### 4.6 التعامل مع الملفات والبيانات الضخمة — Handling Large Blobs
* **المستوى 1:** رفع الملفات مباشرة إلى Object Storage (S3) باستخدام روابط موجهة موقعة مؤقتاً (Pre-signed URLs) لتجاوز خوادم التطبيقات.
* **المستوى 2:** الرفع المجزأ والاستئناف (Multipart Resumable Upload): تقطيع الملف لأجزاء (5-10MB) ورفعها بالتوازي مع إمكانية استئناف الأجزاء الفاشلة فقط عند انقطاع الاتصال.
* **المستوى 3:** إزالة التكرار على مستوى الأجزاء (Chunk-level Deduplication عبر SHA-256). استخدام طلبات HTTP Range عبر شبكات CDN لتشغيل أجزاء الفيديو دون تحميل كامل الملف، واستخدام Erasure Coding لتقليل كلفة التخزين بنسبة 50%.

---

### 4.7 إدارة المهام طويلة التنفيذ — Managing Long Running Tasks
* **المستوى 1:** نمط Task Queue: استقبال الطلب وإرجاع `202 Accepted` مع `task_id`، وسحب المهام ومعالجتها في الخلفية بواسطة Workers (Celery/RabbitMQ).
* **المستوى 2:** استخدام محركات سير العمل الموزعة (مثل Temporal / AWS Step Functions) لحفظ حالة التنفيذ بعد كل خطوة وإمكانية استئناف المهمة بعد الأعطال دون إعادة الحسابات.
* **المستوى 3:** تطبيق الضغط التراجعي (Backpressure)، وتحويل المهام الفاشلة بعد عدة محاولات إلى طابور الرسائل الميتة (Dead Letter Queue - DLQ)، وبث نسبة التقدم للعميل عبر SSE.

---

## 5. Key Technologies — التقنيات المعمارية الأساسية

### 5.1 Redis
* **المستوى 1:** خزانة بيانات في الذاكرة بزمن استجابة ميكروثانية تدعم Strings, Hashes, Lists, Sets, Sorted Sets, Bitmaps, HyperLogLogs.
* **المستوى 2:** محرك أحادي المسار يعتمد على I/O Multiplexing (Epoll) يلغي قفل التنافس. استراتيجيات الحفظ الدائم: RDB (Snapshots سريعة) و AOF (تسجيل كل أمر كتابة).
* **المستوى 3:** توزيع البيانات عبر 16,384 Hash Slot في **Redis Cluster** مع استخدام Hash Tags `{user_123}:profile` لضمان وقوع المفاتيح المترابطة في نفس الخادم الفيزيائي للمعاملات الذرية.

---

### 5.2 Elasticsearch
* **المستوى 1:** محرك بحث موزع مبني فوق Apache Lucene يعتمد على **Inverted Index** لتقطيع النصوص والبحث السريع في مليارات المستندات.
* **المستوى 2:** تقسيم الفهرس لـ Primary و Replica Shards تتألف من قطاعات (Segments) غير قابلة للتعديل. البحث بالتوازي عبر القطاعات مع دمج دوري، ودعم تجميعات إحصائية ضخمة (Aggregations).
* **المستوى 3:** تخصيص 50% كحد أقصى للذاكرة لـ JVM Heap وترك الباقي لـ OS Page Cache. تطبيق بنية الفهارس المتدرجة (Hot-Warm-Cold Architecture) وإدارة دورة حياة الفهارس (ILM).

---

### 5.3 Apache Kafka
* **المستوى 1:** سجل التزام تسلسلي ثابت (Append-only Log). المواضيع (Topics) تنقسم إلى أقسام (Partitions) يكتب فيها المنتجون ويقرأ منها المستهلكون بناءً على الإزاحة (Offset).
* **المستوى 2:** سرعة فائقة بفضل الكتابة التسلسلية، واستخدام Page Cache، وتقنية **Zero-Copy (sendfile)** لنقل البيانات من القرص للشبكة مباشرة. قراءة متوازية عبر مجموعات المستهلكين (Consumer Groups).
* **المستوى 3:** ضبط `acks=all` مع `min.insync.replicas=2` لمنع فقد البيانات. تطبيق معالجة لمرة واحدة بالضبط (Exactly-Once Semantics via Transactional API)، والاعتماد على عمارة **KRaft (Kafka Raft)** للاستغناء عن ZooKeeper.

---

### 5.4 API Gateway
* **المستوى 1:** المدخل المركزي الموحد لكافة طلبات العملاء لتوجيه المسارات (Routing) والمصادقة (JWT Verification).
* **المستوى 2:** إنهاء تشفير SSL/TLS، تقييد معدل الطلبات الموزع (Rate Limiting)، تجميع الاستجابات، وتوليد معرفات التتبع الموزعة (`X-Request-Id`).
* **المستوى 3:** الانتقال لنمط **Service Mesh (Envoy Sidecars)** لإدارة التواصل بين الخدمات الداخلية، وتشفير mTLS تلقائياً، وتطبيق استراتيجيات النشر التدريجي (Canary) وحقن الأخطاء (Chaos Engineering).

---

### 5.5 Apache Cassandra
* **المستوى 1:** قاعدة بيانات NoSQL بأعمدة عريضة لا مركزية (Masterless / Peer-to-Peer) بلا نقطة فشل مفردة، وتتواصل العقد عبر بروتوكول **Gossip**.
* **المستوى 2:** كتابة سريعة في CommitLog على القرص و Memtable في الذاكرة ثم تصديرها كملفات **SSTables** غير قابلة للتعديل، مع استخدام Bloom Filters لتخطي فحص الملفات غير المعنية.
* **المستوى 3:** اتساق قابل للضبط (Tunable Consistency: `ONE`, `QUORUM`, `ALL`). معالجة شواهد القبور (Tombstones) الناتجة عن الحذف، وإصلاح العقد غير المتزامن عبر Merkle Trees.

---

### 5.6 Amazon DynamoDB
* **المستوى 1:** قاعدة بيانات NoSQL مدارة تضمن زمناً بالمللي ثانية بأي حجم، مع مفتاح أساسي بسيط (PK) أو مركب (PK + SK).
* **المستوى 2:** تصميم الجدول الموحد (**Single-Table Design**) لوضع كافة الكيانات في جدول واحد بفهارس ثانوية (GSI) لتنفيذ جميع استعلامات النظام بطلب واحد دون JOINs.
* **المستوى 3:** تفعيل **DynamoDB Streams** لالتقاط التغييرات ومعالجتها بـ AWS Lambda. استخدام **Global Tables** للنسخ المتعدد ثنائي الاتجاه عبر قارات متعددة (Active-Active).

---

### 5.7 PostgreSQL
* **المستوى 1:** قاعدة بيانات علائقية بمعايير SQL الكاملة ومعاملات ACID، مع دعم ممتاز للبيانات شبه المهيكلة عبر حقول JSONB المفهرسة.
* **المستوى 2:** تطبيق **MVCC** لضمان عدم حجب القراءة للكتابة. استخدام مجمع الاتصالات **PgBouncer** لإدارة آلاف الاتصالات وتفادي استنزاف خيوط المعالج.
* **المستوى 3:** فهارس متقدمة: **BRIN** للجداول الضخمة المرتبة زمنياً (توفير 99% من المساحة)، **GIN** للـ JSONB والنصوص، و **GiST** للبيانات الجغرافية (PostGIS). استخدام النسخ المنطقي (Logical Replication) لتغذية CDC.

---

### 5.8 Apache Flink
* **المستوى 1:** محرك معالجة تدفق حقيقي يعامل البيانات كتدفق مستمر لا نهائي بدلاً من تقطيعها لدفعات صغيرة.
* **المستوى 2:** التمييز بين وقت الحدث (Event Time) ووقت المعالجة. استخدام **Watermarks** لمعالجة البيانات المتأخرة وتجميعها داخل نوافذ زمنية صحيحة (Tumbling/Sliding).
* **المستوى 3:** حفظ الحالات في الذاكرة (RocksDB State Backend) وتطبيق خوارزمية **Chandy-Lamport** لأخذ لقطات متسقة (Checkpoints) لضمان المعالجة التامة لمرة واحدة بالضبط (Exactly-Once).

---

### 5.9 Apache ZooKeeper
* **المستوى 1:** مخزن شجري هرمي خفيف في الذاكرة لحفظ الإعدادات المركزية، واكتشاف الخدمات (Service Discovery)، وتسجيل حالة العقد النشطة.
* **المستوى 2:** عقد مؤقتة (Ephemeral Nodes) تختفي تلقائياً عند انقطاع الاتصال لاكتشاف سقوط الخوادم، وعقد متسلسلة (Sequential Nodes) لانتخاب القائد دون ظاهرة التدافع (Herd Effect).
* **المستوى 3:** بروتوكول التوافق **ZAB (ZooKeeper Atomic Broadcast)** لترتيب العمليات تسلسلياً، واشتراط النصاب القانوني ($N/2 + 1$) لمنع انقسام الدماغ (Split-Brain).

---

## 6. Advanced Topics — المواضيع المعمارية المتقدمة

### 6.1 البحث حسب القرب والموقع — Proximity Search
* **المستوى 1:** التحدي: فهرسة بعدين متزامنين (خط الطول ودائرة العرض) دون إجراء مسح كامل لقاعدة البيانات.
* **المستوى 2:** **Geohash** (تحويل الإحداثيات لنصوص تشترك المواقع القريبة في بدايتها Prefix) وشجرة **QuadTree** (تقسيم هرمي يرفع الدقة في المدن المزدحمة ويخفضها في الأرياف).
* **المستوى 3:** الشبكات السداسية **Uber H3** (المسافة من المركز لكافة الجيران متطابقة تماماً لتبسيط خوارزميات الحركة)، والإسقاط الكروي **Google S2** باستخدام منحنيات ملء الفضاء (Hilbert Curve).

---

### 6.2 قواعد بيانات السلاسل الزمنية — Time Series Databases (TSDB)
* **المستوى 1:** بيانات تتألف من `(Timestamp, Metric, Value, Tags)` تمتاز بكتابة تسلسلية هائلة وانعدام التعديل العشوائي.
* **المستوى 2:** خوارزمية ضغط غوريلا (**Gorilla Float Compression**): ضغط الوقت بـ Delta-of-Delta وقيم الأرقام بعملية XOR لتقليل الحجم بنسبة تزيد عن 90% (من 16 بايت إلى 1.37 بايت للنقطة).
* **المستوى 3:** سياسات تقادم هرمية (Downsampling & Tiering): الاحتفاظ بدقة الثواني في Hot Tier (RAM/NVMe) لـ 7 أيام، وتقليلها لمتوسطات دقائق على Warm Tier، ثم متوسطات ساعات على S3 Cold Tier.

---

### 6.3 هياكل البيانات الاحتمالية للـ Big Data — Probabilistic Data Structures

| الهيكل | الوظيفة الأساسية | التعقيد / المساحة | المفاضلة والدقة |
| :--- | :--- | :--- | :--- |
| **Bloom Filter** | فحص وجود عنصر في المجموعة (Set Membership) | $O(k)$ / بتات قليلة لكل عنصر | لا يعطي False Negatives أبداً، ويحتمل نسبة False Positives ضئيلة. |
| **Cuckoo Filter** | فحص الوجود مع دعم إمكانية حذف العناصر | $O(1)$ / مساحة مدمجة جداً | يدعم الحذف الديناميكي بدقة أعلى من Bloom Filter. |
| **HyperLogLog (HLL)** | حساب عدد العناصر الفريدة (Cardinality Estimation) | $O(1)$ / 1.5KB لملايين العناصر | نسبة خطأ قياسية ضئيلة ($pprox 1.04/\sqrt{m}$) دون تخزين العناصر. |
| **Count-Min Sketch** | تقدير تكرار العناصر في التدفق (Frequency Count) | $O(d)$ / مصفوفة ذات حجم ثابت | يضمن عدم إنقاص التقدير أبداً، وقد يزيده قليلاً لتضارب الهاش. |

* **المستوى 1:** استبدال الدقة المطلقة 100% بدقة 99% لتوفير الذاكرة بآلاف المرات عند التعامل مع مليارات السجلات.
* **المستوى 2:** خوارزمية HyperLogLog تعتمد على حساب أقصى عدد من الأصفار المتتالية (Leading Zeros) في ناتج الهاش لتقدير الحجم كـ $2^K$.
* **المستوى 3:** استخدام Bloom Filters في محركات قواعد البيانات لتخطي قراءة ملفات القرص، واستخدام HyperLogLog في Reddit و Twitter لحساب المشاهدات الفريدة فورياً.

---

### 6.4 قواعد بيانات المتجهات — Vector Databases (AI & RAG)
* **المستوى 1:** تحويل النصوص والصور لمتجهات رقمية (Embeddings). البحث يعتمد على التقارب الدلالي بحساب Cosine Similarity أو Euclidean Distance.
* **المستوى 2:** خوارزمية **HNSW** (شجرة طبقات بيانية تتيح القفز بين العناقيد ثم البحث الدقيق بـ $O(\log N)$) وخوارزمية **IVF** (تقسيم الفضاء لخلايا Voronoi).
* **المستوى 3:** تطبيق التكميم **Product Quantization (PQ)** لضغط المتجهات وتوفير 75-90% من الذاكرة، وفصل الفهارس بين الذاكرة والقرص (DiskANN)، وتطبيق البحث الهجين (Dense Vectors + Sparse BM25 Keywords).

---

### 6.5 التقاط تغييرات البيانات — Change Data Capture (CDC)
* **المستوى 1:** مشكلة الكتابة المزدوجة (Dual-Write Problem) وتضارب البيانات عند نجاح كتابة قاعدة البيانات وفشل إرسال رسالة الكاش أو البحث.
* **المستوى 2:** قراءة سجلات المعاملات الأصلية (MySQL Binlog / Postgres WAL) مباشرة عبر **Debezium** وتحويل كل عملية لأحداث تُدفع إلى Kafka بالترتيب الزمني الصارم.
* **المستوى 3:** نمط **Transactional Outbox Pattern** بحفظ الأحداث في جدول محلي داخل نفس معاملة الـ SQL الذرية، ثم قراءتها ونشرها لـ Kafka لضمان اتساق تام بنسبة 100%.

---

## 7. In the Wild — دراسات حالة تطبيقية من كبرى الشركات التقنية

### 7.1 Shopify: حجوزات المخزون في الجمعة البيضاء (Inventory Reservations)
* **المستوى 1 (التحدي):** مئات الآلاف يشترون نفس المنتج ذي المخزون المحدود (Flash Sale) في نفس الثانية مسببين انهيار MySQL بالقفل المتشائم.
* **المستوى 2 (المعمارية):** نقل عدادات المخزون الساخنة إلى **Redis Cluster** واستخدام **Lua Scripts** لخصم المخزون ذرياً في خطوة واحدة (`DECRBY` بشرط ألا يقل عن صفر) وإعطاء المستخدم رمز حجز مؤقت.
* **المستوى 3 (الإنتاج):** تفريغ الحجوزات غير متزامنة في MySQL عبر Kafka، وإعادة المخزون تلقائياً إلى Redis في حال انقضاء مهلة الدفع دون إتمام الشراء.

---

### 7.2 Discord: تخزين تريليونات الرسائل (Message Storage Evolution)
* **المستوى 1 (المسار):** بدأت Discord مع MongoDB $ightarrow$ انتقلت إلى Cassandra لمواكبة مليارات الرسائل $ightarrow$ هاجرت لاحقاً إلى **ScyllaDB**.
* **المستوى 2 (أزمات Cassandra):** توقفات Java Garbage Collection الطويلة (GC Pauses)، تراكم شواهد القبور (Tombstones)، واختناق أقراص NVMe بعمليات الدمج (Compaction).
* **المستوى 3 (الحل في ScyllaDB):** الهجرة لـ ScyllaDB (المكتوبة بـ C++ بنموذج Seastar Thread-per-core الخالي من الأقفال)، وإعادة تصميم مفتاح التقسيم إلى `((channel_id, bucket), message_id)` لمنع تضخم الأقسام عن 100MB، مما حقق زمن استجابة $P99 < 5	ext{ ms}$.

---

### 7.3 Slack: طوابير المهام المليونية (Job Queue Architecture)
* **المستوى 1 (التحدي):** إطلاق مؤسسة ضخمة واحدة لمليون مهمة دفعة واحدة يعطل مهام باقي الشركات الصغيرة في الطوابير المشتركة (Head-of-Line Blocking).
* **المستوى 2 (المعمارية):** بناء محرك **Fair-Queuing** يقسم المهام إلى طوابير افتراضية لكل فريق، وسحب المهام بنظام **Weighted Fair Queuing** لضمان حصة عادلة لكل شركة.
* **المستوى 3 (الإنتاج):** تطبيق حدود استهلاك ديناميكية (Dynamic Rate Limiting) ونقل المهام الزائدة لطوابير منخفضة الأولوية مدعومة بـ Kafka للتخزين المتين.

---

### 7.4 Figma: التعاون اللحظي متعدد المستخدمين (Multiplayer Engine)
* **المستوى 1 (التحدي):** تعديل عشرات المصممين لنفس ملف التصميم في نفس اللحظة ورؤية مؤشرات وتعديلات الجميع فورياً بزمن وصول $< 50	ext{ ms}$.
* **المستوى 2 (المعمارية):** التخلي عن تعقيدات OT وذاكرة CRDT لصالح **خادم تنسيق مركزي أحادي المسار لكل مستند مكتوب بلغة Rust** يحدد الترتيب الزمني الصارم لكافة العمليات.
* **المستوى 3 (الإنتاج):** تطبيق التعديل محلياً وفورياً في المتصفح عبر محرك **C++/WebAssembly** وإرسال العملية للخادم عبر WebSockets ليقوم خادم Rust بدمجها وبثها للجميع وتصحيح أي تضارب طفيف في الذاكرة.

---

### 7.5 Spotify: بحيرة البيانات والتوصيات الموسيقية (Data Lake & Recommendations)
* **المستوى 1 (التحدي):** معالجة مئات الملايين من أحداث الاستماع اليومية لحساب عوائد الفنانين وتوليد قوائم التوصيات المخصصة (Discover Weekly).
* **المستوى 2 (المعمارية):** توجيه الأحداث لـ Google Cloud Pub/Sub $ightarrow$ المعالجة اللحظية بـ **Apache Beam (Dataflow)** $ightarrow$ تفريغ البيانات الخام في بحيرة بيانات (GCS) بصيغة أعمدة مضغوطة **Apache Parquet**.
* **المستوى 3 (الإنتاج):** تشغيل نماذج الترشيح التعاوني (Collaborative Filtering) وتوليد تضمينات موسيقية عبر BigQuery و Spark، وحساب التوافق الدلالي في مجمعات Vector Search لإنتاج قوائم مخصصة لـ 500+ مليون مستخدم كل صباح اثنين.